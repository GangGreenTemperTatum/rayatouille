version: "2"

run:
  timeout: 5m

linters:
  default: none
  enable:
    - errcheck
    - govet
    - staticcheck
    - unused
    - ineffassign
    - gosimple
    - misspell

formatters:
  enable:
    - gofmt

issues:
  exclude-dirs:
    - vendor
