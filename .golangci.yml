version: "2"

run:
  timeout: 5m
  exclude-dirs:
    - vendor

linters:
  default: none
  enable:
    - errcheck
    - govet
    - staticcheck
    - unused
    - ineffassign
    - gosimple
    - misspell

formatters:
  enable:
    - gofmt
